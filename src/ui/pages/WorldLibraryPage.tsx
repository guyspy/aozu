import { Workspace, WorkspaceToolbar, WorkspaceScroll, WorkspaceAdd } from '@/ui/Workspace'
import { useState } from 'react'
import { Link, useNavigate, useParams } from 'react-router'
import { useTranslation } from 'react-i18next'
import { ImageIcon, MapPinIcon, PlusIcon, PencilIcon } from 'lucide-react'
import type { WorldLibraryService } from '@/core/application/world-library'
import { locationAncestors, type WorldLibrary, type LibraryGroup, type AlbumPhoto, type LocationSetting, type LocationCondition, type SettingImage } from '@/core/domain/world-library'
import type { CharacterCollection } from '@/core/domain/character-collection'
import { Button } from '@/ui/components/ui/button'
import { Sheet, SheetContent, SheetTitle } from '@/ui/components/ui/sheet'
import { WorldPicture } from '@/ui/WorldPicture'

type Editing = { kind: 'album' | 'photo' | 'location' | 'condition'; value: LibraryGroup | AlbumPhoto | LocationSetting | LocationCondition; snapshot: WorldLibrary }
export function WorldLibraryPage({ service, library, collections }: { service: WorldLibraryService; library: WorldLibrary; collections: CharacterCollection[] }) {
  const { t } = useTranslation(), text = (key: string) => t(`world.${key}`)
  const { collectionId, locationId, albumId, photoId } = useParams(), navigate = useNavigate()
  const place = library.locations.find((l) => l.id === locationId), album = library.albums.find((a) => a.id === albumId), photo = library.photos.find((p) => p.id === photoId && p.albumId === albumId)
  const [editing, setEditing] = useState<Editing>(), [error, setError] = useState(''), [busy, setBusy] = useState(false)
  const [tag, setTag] = useState('')
  const [reference, setReference] = useState<{ snapshot: WorldLibrary; locationId: string; conditionId: string; photoId: string }>()
  const run = async (task: () => Promise<void>) => { setBusy(true); setError(''); try { await task() } catch (e) { setError(e instanceof Error ? e.message : String(e)) } finally { setBusy(false) } }
  const begin = (kind: Editing['kind'], value?: Editing['value']) => {
    const group = { id: crypto.randomUUID(), name: '', description: '', updatedAt: 0 }
    setError(''); setEditing({ kind, snapshot: structuredClone(library), value: value ?? (kind === 'location' ? { ...group, collectionId: collectionId!, parentId: locationId ?? null, tags: [], consistency: '', images: [], conditions: [] } : kind === 'condition' ? { ...group, images: [] } : group) })
  }
  const save = (data: FormData) => run(async () => {
    if (!editing) return
    const { kind, snapshot, value } = editing, next = structuredClone(snapshot)
    const updated = { ...value, name: String(data.get('name')).trim(), description: String(data.get('description') ?? ''), updatedAt: Date.now() }
    if (kind === 'album') next.albums = [...next.albums.filter((a) => a.id !== value.id), updated]
    if (kind === 'photo') next.photos = next.photos.map((p) => p.id === value.id ? { ...p, ...updated, albumId: String(data.get('albumId')), source: String(data.get('source')) } : p)
    if (kind === 'location') {
      const target = String(data.get('collectionId')), original = value as LocationSetting
      const location = { ...original, ...updated, collectionId: target, parentId: String(data.get('parentId') ?? '') || null, consistency: String(data.get('consistency')), tags: [...new Set(String(data.get('tags')).split(/[,，]/).map((tag) => tag.trim()).filter(Boolean))] }
      // Moving a place also moves its contained places; their local hierarchy is preserved.
      next.locations = next.locations.map((l) => locationAncestors(snapshot, l.id).some((a) => a.id === value.id) ? { ...l, collectionId: target } : l).filter((l) => l.id !== value.id)
      next.locations.push(location)
    }
    if (kind === 'condition') next.locations = next.locations.map((l) => l.id === locationId ? { ...l, conditions: [...l.conditions.filter((c) => c.id !== value.id), updated as LocationCondition], updatedAt: Date.now() } : l)
    await service.save(next); setEditing(undefined)
    if (kind === 'album') navigate(`/albums/${updated.id}`)
    if (kind === 'location') navigate(`/collections/${data.get('collectionId')}/locations/${updated.id}`)
    if (kind === 'photo') navigate(`/albums/${data.get('albumId')}/photos/${updated.id}`)
  })
  const remove = () => run(async () => {
    if (!editing) return
    const { snapshot, kind, value } = editing, next = structuredClone(snapshot)
    if (kind === 'album') { if (value.id === 'default') return; next.albums = next.albums.filter((a) => a.id !== value.id); next.photos = next.photos.map((p) => p.albumId === value.id ? { ...p, albumId: 'default' } : p) }
    if (kind === 'photo') next.photos = next.photos.filter((p) => p.id !== value.id)
    if (kind === 'location') next.locations = next.locations.filter((l) => l.id !== value.id).map((l) => l.parentId === value.id ? { ...l, parentId: (value as LocationSetting).parentId } : l)
    if (kind === 'condition') next.locations = next.locations.map((l) => l.id === locationId ? { ...l, conditions: l.conditions.filter((c) => c.id !== value.id) } : l)
    await service.save(next); setEditing(undefined)
    if (kind === 'album') navigate('/albums')
    if (kind === 'photo') navigate(`/albums/${albumId}`)
    if (kind === 'location') navigate(`/collections/${collectionId}/locations${(value as LocationSetting).parentId ? `/${(value as LocationSetting).parentId}` : ''}`)
  })
  const upload = <label className="world-upload"><PlusIcon size={16} />{text('addImages')}<input type="file" multiple accept="image/png,image/jpeg,image/webp" disabled={busy} onChange={(e) => { const files = Array.from(e.target.files ?? []); e.target.value = ''; if (files.length) void run(async () => { await service.upload(library, albumId ?? 'default', files) }) }} /></label>
  const groupName = (id: string) => id === 'default' ? text('defaultCollection') : collections.find((c) => c.id === id)?.name ?? id
  const albumName = (a: LibraryGroup) => a.id === 'default' ? text('defaultAlbum') : a.name
  const settingImages = (images: SettingImage[], conditionId = '') => <section className="world-grid">{images.map((image) => <article key={image.id} className="world-card"><WorldPicture service={service} hash={image.image.sha256} alt={image.label} /><div><h3>{image.label}</h3><p>{text(image.purpose)}</p><p className="text-xs">{image.source}</p><Button variant="ghost" size="sm" disabled={busy} onClick={() => void run(async () => { const next = structuredClone(library), l = next.locations.find((l) => l.id === locationId)!; const target = conditionId ? l.conditions.find((c) => c.id === conditionId)! : l; target.images = target.images.filter((i) => i.id !== image.id); l.updatedAt = Date.now(); await service.save(next) })}>{text('remove')}</Button></div></article>)}</section>
  if ((albumId && !album) || (photoId && !photo) || (locationId && (!place || place.collectionId !== collectionId)) || (collectionId && !collections.some((c) => c.id === collectionId))) return <Workspace className="world-workspace"><p role="alert">404</p></Workspace>
  const locations = library.locations.filter((l) => l.collectionId === collectionId && (tag ? l.tags.includes(tag) : l.parentId === (locationId ?? null)))
  return <Workspace className="world-workspace" data-workspace-view={collectionId ? 'locations' : 'albums'} data-collection-id={collectionId} data-location-id={locationId} data-album-id={albumId} data-photo-id={photoId} data-has-uncommitted-input={Boolean(editing || reference)}>
    {collectionId && <><nav className="world-tabs"><Link to={`/collections/${collectionId}`}>{text('characters')}</Link><Link aria-current="page" to={`/collections/${collectionId}/locations`}>{text('locations')}</Link></nav></>}
    <WorkspaceToolbar className="world-heading"><div><p className="world-eyebrow">{collectionId ? text('locations') : text('albums')}</p><h1>{place?.name ?? photo?.name ?? (album ? albumName(album) : text(collectionId ? 'locations' : 'albums'))}</h1><p className="whitespace-pre-wrap">{place?.description ?? photo?.description ?? album?.description}</p></div><div className="world-actions">{(place || photo || album) && <Button variant="outline" onClick={() => begin(place ? 'location' : photo ? 'photo' : 'album', place ?? photo ?? album)}><PencilIcon />{text('edit')}</Button>}</div></WorkspaceToolbar><WorkspaceScroll>
    {error && <p role="alert" className="story-error">{error}</p>}{busy && <p role="status">{text('working')}</p>}
    {place && <><div className="world-tags">{place.tags.map((tag) => <span key={tag}>{tag}</span>)}</div>{place.consistency && <section className="world-section"><h2>{text('consistency')}</h2><p className="whitespace-pre-wrap">{place.consistency}</p></section>}<section className="world-section"><div className="world-heading"><h2>{text('images')}</h2></div>{settingImages(place.images)}<WorkspaceAdd><Button variant="outline" onClick={() => setReference({ snapshot: structuredClone(library), locationId: place.id, conditionId: '', photoId: '' })}>{text('addReference')}</Button></WorkspaceAdd></section><section className="world-section"><div className="world-heading"><h2>{text('conditions')}</h2></div>{place.conditions.map((c) => <details key={c.id} className="world-condition"><summary>{c.name}</summary><p className="whitespace-pre-wrap">{c.description}</p><div className="world-actions"><Button size="sm" variant="outline" onClick={() => begin('condition', c)}>{text('edit')}</Button><Button size="sm" variant="outline" onClick={() => setReference({ snapshot: structuredClone(library), locationId: place.id, conditionId: c.id, photoId: '' })}>{text('addReference')}</Button></div>{settingImages(c.images, c.id)}</details>)}<WorkspaceAdd><Button variant="outline" onClick={() => begin('condition')}>{text('createCondition')}</Button></WorkspaceAdd></section></>}
    {collectionId ? <section className="world-section"><div className="world-heading"><h2>{text(place ? 'children' : 'locations')}</h2><label>{text('filterTags')}<select value={tag} onChange={(e) => setTag(e.target.value)}><option value="">{text('all')}</option>{[...new Set(library.locations.filter((l) => l.collectionId === collectionId).flatMap((l) => l.tags))].map((tag) => <option key={tag}>{tag}</option>)}</select></label></div><div className="world-grid">{locations.map((l) => <Link className="world-card" key={l.id} to={`/collections/${collectionId}/locations/${l.id}`}><WorldPicture service={service} hash={l.images.find((i) => i.purpose === 'design')?.image.sha256 ?? l.images[0]?.image.sha256} alt={l.name} /><div><h3><MapPinIcon size={16} />{l.name}</h3><p>{l.description}</p></div></Link>)}</div>{!locations.length && <p className="world-empty">{text('empty')}</p>}</section> : photo ? <><WorldPicture service={service} hash={photo.image.sha256} alt={photo.name} /><p>{photo.source}</p><div className="world-actions"><Button variant="outline" onClick={() => setReference({ snapshot: structuredClone(library), locationId: '', conditionId: '', photoId: photo.id })}>{text('useInLocation')}</Button><Button variant="outline" onClick={() => navigate(`/storyboards?photo=${photo.id}`)}>{text('useInStoryboard')}</Button><Button variant="outline" onClick={() => void run(async () => { const url = URL.createObjectURL(await service.image(photo.image.sha256)), a = document.createElement('a'); a.href = url; a.download = photo.image.filename; a.click(); setTimeout(() => URL.revokeObjectURL(url), 60000) })}>{text('download')}</Button></div></> : <><div className="world-grid">{album ? library.photos.filter((p) => p.albumId === album.id).map((p) => <Link className="world-card" key={p.id} to={`/albums/${album.id}/photos/${p.id}`}><WorldPicture service={service} hash={p.image.sha256} alt={p.name} /><div><h3>{p.name}</h3></div></Link>) : library.albums.map((a) => <Link className="world-card" key={a.id} to={`/albums/${a.id}`}><WorldPicture service={service} hash={library.photos.find((p) => p.albumId === a.id)?.image.sha256} alt={albumName(a)} /><div><h3><ImageIcon size={16} />{albumName(a)}</h3><p>{a.description}</p></div></Link>)}</div>{album && !library.photos.some((p) => p.albumId === album.id) && <p className="world-empty">{text('empty')} {text('imageHint')}</p>}</>}
    <WorkspaceAdd>{!collectionId && !photo && upload}{collectionId ? <Button onClick={() => begin('location')}><PlusIcon />{text('createLocation')}</Button> : !albumId && <Button onClick={() => begin('album')}><PlusIcon />{text('createAlbum')}</Button>}</WorkspaceAdd>
    </WorkspaceScroll>
    <Sheet open={Boolean(editing)} onOpenChange={(open) => { if (!open && !busy) setEditing(undefined) }}><SheetContent className="world-panel overflow-y-auto p-6" aria-describedby={undefined} closeLabel={text('close')} onEscapeKeyDown={(e) => { if (busy) e.preventDefault() }} onPointerDownOutside={(e) => { if (busy) e.preventDefault() }}><SheetTitle>{text('edit')}</SheetTitle>{editing && <form key={editing.value.id} className="world-form" onSubmit={(e) => { e.preventDefault(); void save(new FormData(e.currentTarget)) }}><label>{text('name')}<input name="name" defaultValue={editing.value.name} maxLength={120} required disabled={busy} autoFocus /></label><label>{text('description')}<textarea name="description" defaultValue={editing.value.description} maxLength={8000} rows={4} disabled={busy} /></label>{editing.kind === 'location' && <><label>{text('collection')}<select name="collectionId" value={(editing.value as LocationSetting).collectionId} onChange={(e) => setEditing({ ...editing, value: { ...editing.value as LocationSetting, collectionId: e.target.value, parentId: null } })} disabled={busy}>{collections.map((c) => <option key={c.id} value={c.id}>{groupName(c.id)}</option>)}</select></label><label>{text('parent')}<select name="parentId" value={(editing.value as LocationSetting).parentId ?? ''} onChange={(e) => setEditing({ ...editing, value: { ...editing.value as LocationSetting, parentId: e.target.value || null } })} disabled={busy}><option value="">{text('root')}</option>{library.locations.filter((l) => l.collectionId === (editing.value as LocationSetting).collectionId && !locationAncestors(library, l.id).some((a) => a.id === editing.value.id)).map((l) => <option key={l.id} value={l.id}>{groupName(l.collectionId)} / {locationAncestors(library, l.id).map((a) => a.name).join(' / ')}</option>)}</select></label><label>{text('tags')}<input name="tags" defaultValue={(editing.value as LocationSetting).tags.join(', ')} maxLength={1200} disabled={busy} /></label><label>{text('consistency')}<textarea name="consistency" defaultValue={(editing.value as LocationSetting).consistency} maxLength={8000} rows={6} disabled={busy} /></label></>}{editing.kind === 'photo' && <><label>{text('album')}<select name="albumId" defaultValue={(editing.value as AlbumPhoto).albumId} disabled={busy}>{library.albums.map((a) => <option key={a.id} value={a.id}>{albumName(a)}</option>)}</select></label><label>{text('source')}<textarea name="source" defaultValue={(editing.value as AlbumPhoto).source} maxLength={2000} disabled={busy} /></label></>}<Button disabled={busy}>{text('save')}</Button>{error && <p role="alert" className="story-error">{error}</p>}<Button type="button" variant="ghost" disabled={busy} onClick={() => setEditing(undefined)}>{text('cancel')}</Button>{editing.value.name && !(editing.kind === 'album' && editing.value.id === 'default') && <details><summary>{text('remove')}</summary><p>{text('removeHint')}</p><Button type="button" variant="destructive" disabled={busy} onClick={() => void remove()}>{text('remove')}</Button></details>}</form>}</SheetContent></Sheet>
    <Sheet open={Boolean(reference)} onOpenChange={(open) => { if (!open && !busy) setReference(undefined) }}><SheetContent className="world-panel overflow-y-auto p-6" aria-describedby={undefined} closeLabel={text('close')}><SheetTitle>{text('addReference')}</SheetTitle>{reference && <form className="world-form" onSubmit={(e) => { e.preventDefault(); const data = new FormData(e.currentTarget); void run(async () => {
      const next = structuredClone(reference.snapshot), l = next.locations.find((l) => l.id === String(data.get('locationId'))), p = next.photos.find((p) => p.id === String(data.get('photoId')))
      if (!l || !p) throw new Error('Choose a location and image')
      const target = reference.conditionId ? l.conditions.find((c) => c.id === reference.conditionId) : l
      if (!target) throw new Error('Condition no longer exists')
      target.images.push({ id: crypto.randomUUID(), label: String(data.get('label')).trim(), purpose: data.get('purpose') as SettingImage['purpose'], photoId: p.id, image: { ...p.image }, source: `${p.name} · ${p.source}`.slice(0, 2000) }); l.updatedAt = Date.now()
      await service.save(next); setReference(undefined)
    }) }}><label>{text('chooseLocation')}<select name="locationId" required defaultValue={reference.locationId}><option value="">—</option>{library.locations.map((l) => <option key={l.id} value={l.id}>{groupName(l.collectionId)} / {locationAncestors(library, l.id).map((a) => a.name).join(' / ')}</option>)}</select></label><label>{text('chooseImage')}<select name="photoId" required value={reference.photoId} onChange={(e) => setReference({ ...reference, photoId: e.target.value })}><option value="">—</option>{library.photos.map((p) => <option key={p.id} value={p.id}>{albumName(library.albums.find((a) => a.id === p.albumId)!)} / {p.name}</option>)}</select></label>{reference.photoId && <WorldPicture service={service} hash={library.photos.find((p) => p.id === reference.photoId)?.image.sha256} alt={text('chooseImage')} />}<label>{text('label')}<input name="label" required maxLength={120} /></label><label>{text('images')}<select name="purpose"><option value="inspiration">{text('inspiration')}</option><option value="design">{text('design')}</option></select></label><Button disabled={busy}>{text('save')}</Button>{error && <p role="alert" className="story-error">{error}</p>}</form>}</SheetContent></Sheet>
  </Workspace>
}
